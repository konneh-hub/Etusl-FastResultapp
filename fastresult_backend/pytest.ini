"""
Pytest configuration for FastResult Backend
"""
import os
import django
from django.conf import settings

def pytest_configure():
    settings.configure(
        DEBUG=True,
        DATABASES={
            'default': {
                'ENGINE': 'django.db.backends.sqlite3',
                'NAME': ':memory:',
            }
        },
        INSTALLED_APPS=[
            'django.contrib.contenttypes',
            'django.contrib.auth',
            'django.contrib.sessions',
            'rest_framework',
            'rest_framework.authtoken',
            'accounts',
            'universities',
            'academics',
            'students',
            'lecturers',
            'exams',
            'results',
            'approvals',
            'reports',
            'notifications',
            'files',
            'audit',
        ],
        MIDDLEWARE=[],
        ROOT_URLCONF='',
        SECRET_KEY='test-secret-key',
        USE_TZ=True,
    )
    django.setup()
